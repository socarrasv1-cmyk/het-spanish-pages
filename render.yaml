services:
  - type: web
    name: het-state-scraper-api
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: HET_SCRAPER_API_KEY
        generateValue: true
